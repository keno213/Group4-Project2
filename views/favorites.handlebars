<nav>
  <a href="/search">Back to Search</a>
</nav>
<h1>Favorite Books</h1>
<ul>
  {{#if favorites}}
    {{#each favorites}}
      <li>{{title}}</li>
    {{/each}}
  {{else}}
    <p>No saved favorites yet!</p>
  {{/if}}
</ul>
