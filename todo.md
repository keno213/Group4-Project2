post review
post favorite
the post /search works but the get /search gets the data back in a different model schema. di i need to make the schema the same for both? I do not know


in search.handlebars the api and the model schema are different. I need to make them the same. fix googleId and id

also in the add to favorites needs to be figured out

also connect the script to a review.js and a favorite.js
make sure to add the relations to the db
also must figur out the arry datatype But I think I did as long as I submit an array of 1 to the database when there is 1 author
